let transports: string[] = ["Cycle","Bike","Car"];
transports.forEach(transports => {                         //forEach = it iterate each element and perform same function on each element
    console.log(`I would to own a ${transports}.`);        // use templete literal to call function
});